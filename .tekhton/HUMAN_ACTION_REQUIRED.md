# Human Action Required

The pipeline identified items that need your attention. Review each item
and check it off when addressed. The pipeline will display a banner until
all items are resolved.

## Action Items

## Resolved

- [x] **`CLAUDE.md` § Code Conventions — add doc comment placement rule.** Resolved 2026-05-01: added a `Doc comment placement when inserting items` bullet under `## Code Conventions` → `Doc discipline` in `CLAUDE.md`. Captures the `///`-attaches-to-next-item invariant and notes that `#![deny(missing_docs)]` only catches it on `sdivi-core`.
- [x] [2026-05-01 | Source: architect] **`crates/sdivi-core/src/compute/thresholds.rs` — orphaned `TODO(M09)` for per-category override wiring.** Resolved 2026-05-01 as obsolete: the entry was stale at the time it was filed. Per-category override wiring shipped in M14 (CHANGELOG `[0.1.1]`). Verified: `compute_thresholds_check` reads `cfg.overrides` and applies active overrides at `crates/sdivi-core/src/compute/thresholds.rs:202-235`; `DivergenceSummary` carries `pattern_entropy_per_category_delta` and `convention_drift_per_category_delta`; the "active/unexpired override suppressing a breach" test demanded by the drift log exists at `crates/sdivi-core/tests/compute_thresholds_check.rs:141-158` (`active_override_raises_per_category_limit`) and again at `:183` (`active_override_blocks_per_category_breach`); no `TODO` marker remains in the file. No code change required.
- [x] [2026-05-01 | Source: architect] **CHANGELOG `[0.0.x]` entries with session-style "[MILESTONE N ✓] feat:" titles.** Resolved 2026-05-01: deleted the `[0.0.1]`–`[0.0.16]` blocks entirely. None of those versions were tagged in git or published to crates.io, so there is no audit-trail value in keeping them; the [0.1.0] block already enumerates M01–M13 as the SemVer commitment baseline. Replaced the deleted region with a brief explanatory note pointing readers at git history for the development trail. Also removed the orphan `- [MILESTONE 12 ✓] feat: M12 (M13)` bullet that was trapped inside the [0.1.0] "Binary and bundle sizes" subsection.
- [x] [2026-05-02 | Source: architect] **`bindings/sdivi-wasm/src/types.rs:46–48` — ADL-4 cross-reference on `WasmLeidenConfigInput`.** Resolved 2026-05-02 as obsolete: the action item assumes the "INTENTIONAL GAP (ADL-4)" comment still exists, but M21 (commit `fbd0891`, "Weighted Leiden on WASM") filled the gap. `pub edge_weights: Option<BTreeMap<String, f64>>` was added to `WasmLeidenConfigInput` with `#[serde(default)]` and `#[tsify(optional)]`, and the 3-line gap comment was replaced with a 1-line doc — see CHANGELOG `[0.1.x]` M21 entry and `crates/.../types.rs:46-59`. There is no longer a decision to document via a new ADL entry; the architect's suggested ADL text ("Post-MVP, add `edge_weights`…") describes work that has already shipped. No code change required.
- [x] [2026-05-02 | Source: architect] **`.tekhton/NON_BLOCKING_LOG.md` — three-state checklist format.** Resolved 2026-05-02 as out-of-scope for sdivi-rust: `NON_BLOCKING_LOG.md` is a Tekhton-managed artifact (auto-collected from `${REVIEWER_REPORT_FILE}`); its checklist format is owned by Tekhton's templates and pipeline, not by a downstream consumer. Changing it in-place here would either be clobbered on the next pipeline write or silently diverge from other Tekhton-using repos. The architect raised it against the wrong repo. Filed as a `[FEAT]` note in `../tekhton/.tekhton/HUMAN_NOTES.md` for consideration during a Tekhton-on-Tekhton (dogfood) run.
- [x] [2026-05-02 | Source: architect] **`bindings/sdivi-wasm/src/exports.rs:181-198` — second `IntentDivergenceInfo` assembly seam.** Resolved 2026-05-03 via Option B (chosen by human; pre-1.0 break is acceptable). The fifth parameter of `sdivi_snapshot::assemble_snapshot` (re-exported as `sdivi_core::assemble_snapshot`) was changed from `boundary_spec: Option<&sdivi_config::BoundarySpec>` to `boundary_count: Option<usize>`. Callers that need a count derive it inline (`sdivi-pipeline::pipeline.rs` does `boundary_spec.as_ref().map(|s| s.boundaries.len())`); the WASM wrapper passes its `boundary_count: Option<u32>` field straight through. The post-call `snap.intent_divergence = …` override block in `bindings/sdivi-wasm/src/exports.rs` is deleted, restoring a single canonical assembly seam. Side effect: `sdivi-snapshot` no longer depends on `sdivi-config`. Workspace version bumped to `0.2.0`; `@geoffgodwin/sdivi-wasm` to `0.2.0`. Recorded as ADL-10 in `.tekhton/ARCHITECTURE_LOG.md`; CHANGELOG `[0.2.0]` and MIGRATION_NOTES `0.1.x → 0.2.0` document the breaking change. Snapshot JSON output is byte-identical for the same inputs. `cargo build --workspace` and `cargo test --workspace` both pass clean (one pre-existing `workspace_version_is_v0_semver` assertion was relaxed from `starts_with("0.1.")` to `starts_with("0.")` so the test reflects the v0 contract rather than a specific minor).
- [ ] [2026-05-04 | Source: architect] **`crates/sdivi-parsing/tests/import_extraction.rs` placement vs CLAUDE.md convention** — CLAUDE.md (Testing Strategy section) states: "workspace-level cross-crate tests" must live in the top-level `tests/` directory. `import_extraction.rs` uses `sdivi-parsing`, `sdivi-graph`, and all six language adapters, making it cross-crate by that definition. It was placed in `crates/sdivi-parsing/tests/` because `CARGO_MANIFEST_DIR`-relative fixture paths resolve cleanly from that crate root. Decision required: **(a)** accept the deviation and add an explicit exception to CLAUDE.md — "cross-crate tests that require `CARGO_MANIFEST_DIR`-relative fixture resolution may live in the crate whose manifest root makes the path shortest"; or **(b)** migrate the file to `tests/` at the workspace root and resolve fixtures via `env!("CARGO_MANIFEST_DIR")` from there (requires verifying that `tests/fixtures/` is reachable from the workspace root manifest directory, which it is). Either answer is acceptable; the current state drifts from the written convention and should be ratified or corrected deliberately. ---
