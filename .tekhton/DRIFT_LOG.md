# Drift Log

## Metadata
- Last audit: 2026-04-28
- Runs since audit: 1

## Unresolved Observations
- [2026-04-29 | "Implement Milestone 3: Parsing Stage with One Language Adapter (Rust)"] `crates/sdi-lang-rust/src/lib.rs:66,86` — the `ACTIVE_TREES` doc comment says adapters "increment on tree creation and decrement on drop," but the code decrements after the `PARSER.with` closure returns rather than on `tree`'s `Drop`. The invariant is correctly tested (counter is 0 when `parse_file` returns), but the comment is misleading.
- [2026-04-29 | "Implement Milestone 3: Parsing Stage with One Language Adapter (Rust)"] `crates/sdi-lang-rust/src/extract.rs:56-73` — `extract_exports` does not stop traversal when entering an `EXPORTABLE_KINDS` node. A `pub fn` inside a `pub mod` will appear in `exports` twice: once when the `mod_item` is visited and once when the `function_item` is visited. Latent correctness issue for the patterns and graph stages that consume `FeatureRecord.exports`.
- [2026-04-29 | "Implement Milestone 3: Parsing Stage with One Language Adapter (Rust)"] Carried from M02 (still unresolved): TOCTOU on `path.exists()` + `read_to_string` in `load.rs:98` and `boundary.rs:60`; `load.rs:111` key formatted with `{key}` not `{key:?}` (ANSI escape risk); `validate_date_format` accepts semantically invalid day values (e.g. `2026-02-30`); `init.rs:84` `--force` hint not wired into clap; `init.rs:63-68` duplicate `SDI_CONFIG_PATH` lookup.
- [2026-04-28 | "architect audit"] The following observations stay in DRIFT_LOG for future cycles:
- [2026-04-28 | "architect audit"] **`clap = ">=4.4, <4.5"` version restriction** (M02 observation) — intentionally deferred to M11 per coder. No action this cycle. Retains its place in DRIFT_LOG as a pre-M11 reminder.
- [2026-04-28 | "architect audit"] **`crates/sdi-cli/src/output/mod.rs` empty `pub mod json` / `pub mod text`** (M01 and M02 observations, duplicates) — the file's own comment says "Output formatters (text tables and JSON) are added in Milestone 8." These are intentional scaffolding. No action until M08. Both observations can be removed from DRIFT_LOG once M08 begins, or resolved at M08 completion.
- [2026-04-28 | "architect audit"] **`crates/sdi-config/src/lib.rs` missing `#![deny(missing_docs)]`** (M01 observation) — CLAUDE.md mandates `#![deny(missing_docs)]` on `sdi-core` only. `sdi-config` is not in scope for this requirement. All current public items do have doc comments. No action required.

## Resolved
- [RESOLVED 2026-04-29 | post-M03 chore commit] **`init.rs` stdout/stderr contract** — moved all `sdi init` `println!` calls to `eprintln!` and updated the two `init.rs` integration assertions to `.stderr(...)`. Decision: option (b). CLAUDE.md Rule 8 unchanged. Closed before 0.1.0 stdout/stderr contract lock.
- [RESOLVED 2026-04-29 | post-M03 chore commit] **`serde_yaml` unmaintained** — swapped workspace dep to `serde_yml = "0.0.12"` (maintained fork). Single call site in `crates/sdi-config/src/boundary.rs:64` updated. Closes both the M01 entry and the M02 follow-up.
- [RESOLVED 2026-04-28] `thresholds.rs` is declared `pub(crate)` at module level but its functions (`today_iso8601`, `is_expired`, `validate_date_format`, `validate_and_prune_overrides`) are all `pub`. They are unreachable from outside the crate regardless; tightening to `pub(crate)` removes the mismatch.
- [RESOLVED 2026-04-28] `init.rs` writes progress lines (`"sdi: created .sdi/config.toml"`, `"sdi: detected languages: ..."`) to stdout. Rule 8 reserves stdout for snapshot JSON, summaries, and table output and assigns progress/status to stderr. The integration tests pin these on stdout, so this is intentional, but the choice is worth flagging before 0.1.0 stdout/stderr contract is locked.
- [RESOLVED 2026-04-28] `load.rs:122-131` has an `else {}` block with only a comment and no code; clippy's `clippy::redundant_else` or empty-block lint may or may not fire depending on the version. Worth verifying in CI.
- [RESOLVED 2026-04-28] `Cargo.toml` (carried from M01): `clap = ">=4.4, <4.5"` restricts to 4.4.x only, blocking security patches in 4.5+. Deferred to M11 per coder; must not be forgotten.
- [RESOLVED 2026-04-28] `crates/sdi-cli/src/output/mod.rs` (carried from M01): `pub mod json` and `pub mod text` are exposed with empty bodies. Ensure they receive content or become private before 0.1.0 publish.
- [RESOLVED 2026-04-28] `Cargo.toml:41` — `serde_yaml = "0.9"` is unmaintained (last release 2023-06). Coder added `# note: unmaintained upstream; revisit in M10` comment. No action needed now; revisit in M02 when the YAML loader is written.
- [RESOLVED 2026-04-28] `crates/sdi-cli/src/output/mod.rs` — `pub mod json` and `pub mod text` are exposed with empty bodies. IDEs and rustdoc will surface them as public API with no content. Low-risk for a scaffold; ensure they receive content (or become private) before the first `0.1.0` publish.
- [RESOLVED 2026-04-28] `crates/sdi-config/src/lib.rs` — `#![deny(missing_docs)]` is absent. All public items currently have doc comments, but there is no compile-time enforcement. CLAUDE.md mandates it on `sdi-core` only; no action required, but worth adding before the config surface stabilizes.
