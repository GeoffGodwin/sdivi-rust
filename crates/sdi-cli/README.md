# sdi-cli

Command-line interface for the [Structural Divergence Indexer](https://github.com/geoffgodwin/sdi-rust).
Produces the `sdi` binary.

```sh
cargo install sdi-cli
sdi --help
```

Part of the `sdi-rust` workspace. See the [workspace README](../../README.md) for full documentation.
