# sdivi-cli

Command-line interface for the [Structural Divergence Indexer](https://github.com/geoffgodwin/sdivi-rust).
Produces the `sdivi` binary.

```sh
cargo install sdivi-cli
sdivi --help
```

Part of the `sdivi-rust` workspace. See the [workspace README](../../README.md) for full documentation.
